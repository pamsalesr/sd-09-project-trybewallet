export const saveEmail = (email) => ({ type: 'SAVE_EMAIL', email });

export const sendObj = (obj) => ({ type: 'SEND_OBJ', obj });

export const sendExpenses = (expenses) => ({ type: 'SEND_EXP', expenses });
